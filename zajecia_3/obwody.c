#include <stdio.h>

int main(){
    int a,b,c;
    char f;
    scanf("%c", &f);
    if(f= 'p');{
        scanf("%i %i", &a, &b);
        printf("%i \n",  a*b);
        return 0;
    }
    if(f= 'k');{
        scanf("%i",&a);
        printf("%i \n",  a*a);
        return 0;
    }
     if(f= 't');{
        scanf("%i %i ",&a,&b);
        printf("%i \n" , a*b/2);
        return 0;
    }
       

}